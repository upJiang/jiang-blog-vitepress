# 小江 AI 博客写作规范

本文件与 `AGENTS.md` 保持相同要求，供 Claude Code 使用。项目级规范是内容质量的唯一判断依据；脚本只负责格式、链接、路由和报告完整性，不能判断文章是否讲清楚。不限制字数、章节数和表达形式，每篇文章根据主题和材料决定篇幅与结构。

## 读者与目标

- **每个栏目面向该领域的零基础小白**。写后端文章时，读者是后端小白；写 SEO 文章时，读者是 SEO 小白；写 AI 与 Agent 文章时，读者是想转 AI 应用开发的技术人员。读者有通用编程基础，但不懂该领域的专业概念。
- 不要假设读者是前端、后端或任何特定背景，也不要在一篇后端文章里假设读者懂 LLM、在一篇 AI 文章里假设读者懂数据库。
- 让读者真正学到东西：读完能复述核心机制、能判断边界、能把知识迁移到自己的项目。
- 技术博客：专业和通俗要同时在场。
- **写法参照**：`docs/ai-agent/llm-workflow-rag-agent.md` 是写作范本。各栏目写作时参照它的写法：概念纵向挖深的程度、口语与专业的融合方式、承上启下的段落衔接、排版与代码的处理。参照它的写法，而不是复制它的结构。

## 深度的定义：纵向挖，不是横向铺

- **纵向深度**：把一个概念挖到根，讲定义、底层机制、真实参数、边界条件、失败反例。
- **横向铺开**：不断引入新概念、每个只讲两句，是反面。
- 宁可一篇只讲透两个概念，也不要十个概念各一段话。
- 概念讲到什么程度，由它在论证中的重要性决定。核心概念讲透，背景概念点到为止。
- **不限制字数**。为深度而长，不为长度而长。

## 每个概念的标准讲法

1. **定义**：一句话说清。术语首次出现用 **英文（中文）** 格式。
2. **机制**：内部怎么运转，讲到具体参数、数据结构、算法步骤。
3. **边界与反例**：什么情况下失效，防止读者把机制神化。
4. **为什么**：它与主问题的关系，解释了哪个现象、影响了哪个判断。

概念之间不一定是因果关系。写作前先判断它们属于因果、包含、组成、组织、计量、容量限制、时间顺序、依赖或对比中的哪一种关系，再决定展开顺序。只有当前一个机制确实暴露出能力缺口时，才使用能力演进链。禁止为了模仿范文，把并列视角或上下位概念硬写成前后步骤。

标题同时出现多个术语时，必须逐一解释术语，并说清每一对关键关系。比如 Message、Token 与 Context 应先区分内容、组织方式、计量单位和容量边界，不能写成 `Message → Token → Context` 这样的伪流程。关系说不清时先查证、改标题或拆文，不能进入正文。

## 文风

- 术语不丢，口语搭桥。口语负责解释"为什么"，不替换术语。
- 承上启下：每个概念的出现都有依据。衔接可以来自同一个对象、同一次请求、同一组证据、概念间的真实关系或前文暴露出的失败，不强制写成因果过渡句。
- 允许下判断，判断要有理由。不编造数据、指标、事故、个人经历。

禁用表达：

- 不用中文或西文破折号
- 不用"逼"这类用力过猛的词，用"引发""导致"
- 不用翻案腔（"不是 A，而是 B"）
- 不用报菜名（"四个词：A、B、C、D"）
- 不用对仗排比
- 不用俗套转折（"你会发现""几乎绕不开"）
- 不用"起点/补救一/钉子一"这类编排腔标题

标题：问题式或机制式；不从正文抽结论句当标题；标题之间读起来能看出论证路径。

## 排版

- Mermaid 图要小：节点 3 到 6 个、标签短，正文讲过的不要重复画。
- 表格用于对比、参数、分档；已全局配置自适应宽度，不在单元格里塞超长文本。
- 提示块（:::）只用于真正值得独立强调的重点或总结。
- 只加粗确实需要承重的短语，禁止整段加粗、机械加粗段首和连续对称加粗。强调标记与后续中文正文之间保留空格，例如 `**Message（消息）** 是……`。最终以浏览器渲染结果为准，页面出现裸露的 `**` 即判定失败。
- 分段：一个概念一段或几段，不写一泻千里的大段。

## 代码

- 块前交代场景、输入、目标。
- 块内只在关键分支、状态变化、返回值处写中文注释。
- 块后解释职责、调用顺序、结果、异常和边界。
- 可运行示例必须实际运行验证过；伪代码要明确标注"示意伪代码"。
- Fake Adapter、脚本化模型、内存仓库只能作为测试替身，正文明确它们不能证明真实 API 或供应商行为。

## 事实与安全

- 事实来自官方文档、规范、公开源码、测试，或匿名化的实现证据。
- 私有项目（tp-knowledge、seoOpt 插件、interview-project 等）只能作为只读取证。公开文章不得出现项目名、本机路径、内部接口、表结构、真实数据、提示词、截图或凭证。
- 写作卡、证据台账、自测记录只放 `/tmp`，完成后删除。
- 完稿后从零基础读者和资深工程师两个角度各复读一遍：主题讲清、论证连续、示例可信、语言自然。

## 结构连续性与文章质检

这部分是写作门槛，不是完稿后的装饰性检查。文章即使术语准确、代码可运行，只要论证链断裂，也不能视为完成。

### 正文之前先通过概念关系门禁

文章契约不是第一步。作者必须先在 `/tmp/jiang-blog-rewrite/<content-id>/concept-model.md` 建立概念模型，未通过独立验证前不得写标题树和正文。概念模型至少包含：

- 从主标题中拆出的对象、动作、限定词和关系词。
- 每个核心概念的一句话定义、所属层级、输入输出或观察对象，以及可靠来源。
- 概念之间的真实关系。明确它们是因果、包含、组成、组织、计量、容量、时序、依赖还是对比；不存在的关系也要写明。
- 一句话总模型。零基础读者只读这句话，就能知道这些概念为什么会同时出现在标题里。
- 一个最小反例。用来证明读者不会把相邻概念误认为同义词、上下位概念或先后步骤。

概念模型必须能回答“标题为什么把这些词放在一起”。任一术语未定义、关系依靠猜测、来源互相冲突或一句话总模型无法成立时，状态保持 `rewrite_required`。此时只能继续查证、缩小主题、改标题或拆分文章，禁止用流畅文字掩盖概念问题。

### 标题承诺必须逐项兑现

概念模型通过后建立标题兑现表：核心术语要有定义、机制和边界；术语之间的连接词要有关系说明或对照实验；副标题中的问题要在正文中得到直接回答；面向读者的动作要有可执行的判断、实现或验证方法。

标题使用“X、Y 与 Z”“从 X 到 Y”或“X：Y”时，连接词本身也是承诺。正文不能只分别介绍 X、Y、Z，也不能用一张术语表代替关系解释。若文章实际内容与标题不一致，优先修改标题或重构正文，不保留一个搜索词齐全但无法兑现的标题。

### 根据主题选择论证主线

范文使用的是能力缺口链，因为 RAG、Workflow 与 Agent 确实依次解决前一阶段留下的问题。其他文章必须选择最符合主题的结构，不能默认套用范文的因果链：

- 能力演进：现有能力失败，新的机制补上缺口。
- 关系解释：先确定共同观察对象，再分别解释各概念从哪个角度描述它。
- 请求或数据流：沿真实输入、处理、状态变化和输出展开。
- 对照实验：只改变一个变量，比较结果，借差异解释机制。
- 诊断链：症状、假设、证据、定位、修复和回归。
- 推导链：从约束、不变量或已知条件推到实现与复杂度。

案例也不强制写成状态机。只有主题真的涉及状态变化时才使用状态机；关系型概念优先使用同一请求的多次对照，算法优先使用不变量和反例，诊断文优先使用证据逐步收敛。结构必须服从主题，不能让主题服从模板。

### 一篇文章只能有一条主问题链

- 写作前先写一句“这篇文章要帮读者解决什么问题”，全文围绕这句话推进。
- 核心概念控制在能够讲透的范围内。新概念只有在主问题要求、概念关系需要或贯穿载体产生新信息时才进入正文；与主问题无关的概念拆成其他文章或只留链接。
- 复杂主题可以有多个小节，但不能把定义、架构、工具、测试、生产治理和 FAQ 平铺成知识清单。
- 每篇文章至少保留一个可回放的贯穿载体，如具体任务、同一请求、对照实验、数据结构、故障证据或算法不变量。载体要在关键小节提供新信息，不能只在开头出现一次。

### 标题树必须读得出论证路径

- 先检查标题树，再检查正文。只读 `#`、`##`、`###` 标题时，读者应能辨认所选主线，并看出每节为什么出现在当前位置。
- 当前标题必须延续所选论证主线：回答上一节留下的问题、继续观察同一对象、解释刚出现的差异，或明确缩小到下一个机制。不能从概念突然跳到代码、测试、缓存或生产运维。
- 标题优先使用问题式或机制式表达，例如“它解决了哪一个缺口”“什么情况下会失效”“状态由谁保存”。不要使用只表示分类的标题，例如“相关概念”“其他问题”“一个示例”。
- 一个 `##` 下的 `###` 必须属于同一层论证。若小节可以独立成为另一篇文章，应拆分，而不是在当前文章中顺带介绍。

### 段落之间要有可解释的过渡

- 每个新段落的第一句都要能回答“为什么现在讲这个”。如果只能用“此外”“另外”“同时”连接，通常说明语义关系还没有写清楚。
- 自然承接来自语义关系，不来自桥接句。可以沿用前文的同一个输入、对象、状态或观测结果，也可以让下一节回答前文自然产生的问题。禁止反复使用“A 解决了……但还不能……所以需要 B”来制造衔接。
- 一节结束时不要重复本节结论，也不强制补一条“承上启下”的句子。删掉过渡句后，如果两节仍能靠事实和标题自然相接，过渡句应删除；如果删掉后关系消失，缺的是论证内容，应重排材料，而不是再写一句连接词。
- 表格、流程图和代码块不能代替上下文。块前交代它要解决的具体问题，块后解释输出怎样改变后续控制流。
- 文章结尾要回到主问题，并把读者交给下一篇的明确问题。禁止统一套用与主题无关的“机制复核”“迁移复核”“性能与安全权衡”等通用尾段。

### 例子只保留能推动理解的细节

- 例子优先使用读者熟悉的自然材料，如用户问题、对话历史、文档片段、函数输入、数组或一次 HTTP 请求。每个字段都必须参与后续推理。
- 禁止为了营造工程感编造精确 ID、版本号、租户、策略名、审计号、时间戳和内部状态名。`access_request:1842:v42` 这类字符串如果删掉不影响机制，就不应出现。
- 只有来源明确且机制确实依赖该字段时，才保留精确值。教学占位值必须清楚标注，不得暗示它来自真实生产系统。
- 引用私有项目只能提炼已核实的通用机制，不能把内部命名改头换面后伪装成公开案例。

### 深度要落到机制，而不是增加名词

- 核心概念至少交代定义、内部机制、一个失败反例和它在主论证中的作用。
- 代码示例要对应正文刚刚提出的机制，并解释输入、状态变化、输出、异常和边界。不能用一段孤立代码制造“已经深入”的感觉。
- 一篇文章宁可完整推演一次请求、任务、数据结构或故障恢复，也不要用十个小节各介绍一个名词。
- 深度不等于字数。删掉不改变判断、机制或操作的重复解释；新增内容必须带来新证据、新边界或新决策。

### 范文的使用边界

- `docs/ai-agent/llm-workflow-rag-agent.md` 只作为论证深度、叙述节奏、案例推进和排版质量的参照，不是固定目录模板，也不是每篇文章都要复制的开场、表格、提示块和结尾。
- 参照范文时，先写清它为什么适合能力缺口链，再判断当前主题是否属于同一类型。若当前主题是关系解释、对照实验、请求流或推导链，应重新选择结构，不能把范文的因果链、过渡句或章节数量搬过来。
- 参考范文必须对比四件事：开头读者缺什么、每节新增了什么证据、贯穿对象如何变化、结尾获得了什么判断能力。只模仿小标题、代码数量、段落长度或“A 还不够，所以讲 B”的句式，视为没有参考范文。
- 不用“统一结构”替代主题判断。文章类型不同，概念文、实现文、诊断文和架构文可以有不同结构，但都必须有清晰的主问题和承接关系。

### 批量重写不能变成模板拼接

- 禁止对多篇文章机械追加相同的总结、质检、迁移或工程建议段落。共享规则应写入本文件，不应复制进每篇正文。
- 批量处理只能用于 frontmatter、链接、格式等确定性修改。正文重写必须逐篇确认主问题、案例、标题树、核心机制和结尾承接。
- 批量完成后不能只检查文件数量、字数、标点和构建。每篇都要检查标题兑现、概念关系、首尾段、贯穿载体和重复内容，抽查不能代替逐篇审查。
- 发现一篇文章结构失控时，先修概念模型、写作卡和质检流程，再继续批量写作，不能用更多文章覆盖问题。

### 从概念模型到成稿

概念关系门禁通过后，才按下面的顺序设计文章：

- **文章契约**：在 `/tmp` 写下主问题、读者读完后的动作、标题承诺、已验证的概念关系、论证主线、核心材料、失败反例、必须展示的代码或数据结构、明确不讲的内容和下一篇问题。
- **贯穿载体**：根据主题选择案例状态、同一请求、同一数据结构、对照实验、故障证据或算法不变量。载体必须在关键小节产生新信息，不能只在开头举例。
- **标题论证化**：标题树应清楚呈现所选主线。能力演进文写缺口和机制，关系文写观察对象和概念关系，诊断文写证据如何收敛，算法文写不变量与推导。不要把术语排成并列清单。
- **机制六层**：定义、内部运转、输入输出或状态变化、在案例中的作用、失败责任、验证方式。只讲定义和优点不算讲透。
- **可回放主线**：按主题选择知识流、控制流、信任流、时间/版本流、请求生命周期或变量对照。模型、检索、工作流、Agent 类实现文通常需要写清“输入装配 → 调用/执行 → 观察 → 输出解析 → 业务终态”，概念关系文不强制套用这条流程。
- **材料过渡**：每节新增的事实、状态、对照结果或边界应自然决定下一节讲什么。过渡句只负责消除歧义，不能代替缺失的关系。
- **证据块**：图表、代码前说明场景、输入和目标，之后解释职责、控制流、状态变化、异常、边界和测试意义。Fake 模型、内存仓库、适配器只能证明控制逻辑，不能证明真实供应商行为。
- **信任边界**：区分用户输入、外部文档、检索证据、工具回执、模型候选与程序维护的身份、权限、版本、终态。模型建议不等于执行，检索命中不等于有权查看，响应成功不等于事实正确。
- **故障回放**：说明故障属于装配、调用、检索、流程、Agent 循环、输出验证还是业务终态，并记录相关状态、版本、证据和停止原因。不能把所有问题归因于 Prompt。
- **事实分级**：区分实测值、官方约束、经验参数和教学近似。没有证据的阈值、比例、性能数字和行业结论不能写成普遍规律，示例值须标明范围或仅用于演示。
- **结尾闭环**：回答开头主问题，说明解决了什么和仍有什么边界，再提出下一篇要回答的具体问题。禁止批量套用无关的总结或工程建议段落。

## P0 排版与独立第三者门禁

- 普通段落超过约 240 个中文字符、同时承载多个概念或动作、表格单元格塞入长方案、代码/图表前后缺少解释、标题树读不出论证路径时，文章不得交付。
- 桌面正文列保持受控宽度，移动端不得出现页面级横向滚动、文字裁切、目录遮挡或按钮挤压；固定检查 375px、768px、1024px、1440px。
- `docs/ai-agent/llm-workflow-rag-agent.md` 只读，作为质量基线。其余文章必须完成概念模型、文章契约、自测、独立第三者检查和浏览器渲染检查。
- 独立第三者必须来自不同 Agent 或不共享作者推理过程的全新上下文。第一轮盲审只读取文章标题、成稿和必要的相邻文章，先独立复述概念定义、概念关系、主问题和标题树；第二轮再读取契约与事实来源核验。作者不得为自己的文章出具第三者 `pass`。
- `yarn article:audit` 只能检查标题、围栏、段落、重复内容和站内路由；`yarn article:third-party` 只能校验独立报告是否存在、结构是否合法、证据是否带位置。任何脚本都不得生成或推断语义 `pass`。
- 语义审查通过后必须打开真实页面检查。四个视口都要确认标题、正文、表格、代码和目录无溢出，并检查页面没有裸露的 `**`、失效提示块、错误代码围栏或异常换行。未完成浏览器检查不得标记 `pass`。
- 任一视角为 `rewrite_required` 就从概念模型、论证主线、贯穿载体、标题树和结尾重新设计；链接、格式、错别字和局部事实标注问题才可局部修复。
- 用户或第三者发现概念模型错误、标题不兑现或同类系统性问题时，立即暂停后续批量写作。当前文章及使用同一错误方法得到的 `pass` 全部退回 `rewrite_required`，先修规范和方法，再重新审查。
- 质检报告、失败草稿、截图和写作卡仅放 `/tmp`，完成后删除。

## 各栏目定位

- **AI 与 Agent**：12 个阶段、98 篇正文的唯一学习主线；AI 实践 6 个阶段、10 篇。`stageKey`、`sequence`、`dependsOn`、`sourceKey`、栏目 Tab、左侧目录、上一篇/下一篇必须来自同一课程模型（`.vitepress/ai-agent-curriculum.ts` 与 `.vitepress/content.ts`）。
- **前端、后端、SEO、AI Infra**：按主题写，重点把概念讲透、让读者能迁移，不追求知识点清单。
- **算法与重学前端**：保留原有主题范围和学习目标，但正文、frontmatter、标题、代码、案例与链接同样纳入全量重写。不得以 `contentLocked` 或“原文已有内容”为免检理由。
- 公开品牌为 `小江AI`，不用"课程平台""课程式技术博客"文案。
- 不强制展示统一的"参考资料"章节。

## 流程

1. 从标题和可靠来源建立概念模型，明确概念定义、层级、关系、不成立的关系和一句话总模型。
2. 由独立上下文验证概念模型。未通过时继续查证、改标题、拆文或缩小范围，不写正文。
3. 建立标题兑现表，选择适合主题的论证主线和贯穿载体，再写文章契约与标题树。
4. 只使用有来源或明确标注的材料从头写正文，不复用旧结构，不添加没有证明作用的精确工程细节。
5. 作者自测标题兑现、概念关系、材料推进、事实、代码、自然表达和排版，结果只允许 `ready_for_third_party` 或 `rewrite_required`，作者不能给自己语义 `pass`。
6. 独立第三者先盲审成稿，再结合契约和来源核验；通过后执行 375px、768px、1024px、1440px 浏览器渲染检查。
7. 完稿后用 `article-write` 和 `humanizer-zh` 处理模板腔、机械过渡、重复转折、宣传语和过度粗体。润色不能改变概念模型，也不能替代语义审查。
8. 用户只要求质检、审查、对比或讨论原因时，先执行只读审查，不修改正文、标题、导航或样式。
9. 全站审查不能用一篇文章推断全部文章。逐篇阅读和审查；未读文章只能标为待审查，不能宣称已完成全面质检。
10. 用户确认全量改写后，所有非范文正文均进入 `rewrite_required` 队列。批次只表示执行顺序，不表示内容通过；任何系统性失败都会暂停队列并退回同方法产出的结果。

## 全量深度重写执行门禁（覆盖此前冲突表述）

- “完成重写”必须同时具备：通过独立验证的概念模型、标题兑现表、文章契约、全新标题树、正文、作者自测记录、独立第三者三视角 `pass` 和浏览器渲染证据。Git 差异、字数变化、构建成功和静态脚本通过都不能单独证明重写完成。
- 范文 `docs/ai-agent/llm-workflow-rag-agent.md` 是唯一只读正文。其他正文默认初始状态为 `rewrite_required`，包括算法与重学前端。
- 文章契约字段固定为：主问题、读者动作、标题承诺、已验证的概念关系、论证主线、贯穿载体、核心材料、失败反例、证据、代码/图表、明确不讲内容、前后文章承接和路由。
- 标题树必须符合所选论证主线，并能兑现标题中的每个对象和关系。标题不兑现、概念关系错误、贯穿载体失效、章节缺少语义承接或出现长文本墙时，必须废弃草稿并从概念模型重新开始。
- `article:audit` 和 `article:third-party` 是格式/结构辅助门禁。第三者审查必须独立读取契约、成稿和事实来源，输出带行号的语义证据；脚本不得把 H1、围栏、链接或段落长度推断为内容 `pass`。
- `repair_allowed` 只允许错别字、链接、格式、代码注释和局部事实标注。涉及标题承诺、主问题、概念关系、论证主线、贯穿载体、失败边界、结尾闭环或排版结构，一律 `rewrite_required`。
- 任何 `rewrite_required` 都必须把失败稿保留到 `/tmp/jiang-blog-rewrite/<content-id>/failed/`，重新建立概念模型、契约和标题树，禁止在旧结构上局部修补。
- 课程依赖以 `dependsOn` 为唯一依据，课程顺序、侧边栏、上一篇、下一篇、栏目索引和内链必须由同一模型生成。改名、拆分、合并或删除文章时必须同步 canonical、Sitemap 和旧路由重定向/404 策略。
- 每篇文章必须检查 375px、768px、1024px、1440px；普通段落多概念、表格塞长方案、代码/图表缺少解释、移动端溢出或桌面正文过宽均不得交付。

## 站点纪律

- 不执行 `git add`、`git commit`、`git push`。
- 不修改自动部署配置。
- 文章 frontmatter 必须与 `.vitepress/content.ts` 清单一致（title、description、category、part、chapter、tags 等字段），这是站点导航的数据源，不是写作门禁。
