# 小江AI 博客写作规范

本文件与 `AGENTS.md` 保持相同要求，供 Claude Code 使用。此前所有门禁脚本已删除，本文档是唯一的写作标准。不限制字数、不限制章节数、不限制表达形式，每篇文章根据主题和内容决定自己的篇幅与结构，避免千篇一律。

## 读者与目标

- **每个栏目面向该领域的零基础小白**。写后端文章时，读者是后端小白；写 SEO 文章时，读者是 SEO 小白；写 AI 与 Agent 文章时，读者是想转 AI 应用开发的技术人员。读者有通用编程基础，但不懂该领域的专业概念。
- 不要假设读者是前端、后端或任何特定背景，也不要在一篇后端文章里假设读者懂 LLM、在一篇 AI 文章里假设读者懂数据库。
- 让读者真正学到东西：读完能复述核心机制、能判断边界、能把知识迁移到自己的项目。
- 技术博客：专业和通俗要同时在场。
- **写法参照**：`docs/ai-agent/llm-workflow-rag-agent.md` 是写作范本。各栏目写作时参照它的写法：概念纵向挖深的程度、口语与专业的融合方式、承上启下的段落衔接、排版与代码的处理。参照它的写法，而不是复制它的结构。

## 深度的定义：纵向挖，不是横向铺

- **纵向深度**：把一个概念挖到根，讲定义、底层机制、真实参数、边界条件、失败反例。
- **横向铺开**：不断引入新概念、每个只讲两句，是反面。
- 宁可一篇只讲透两个概念，也不要十个概念各一段话。
- 概念讲到什么程度，由它在论证中的重要性决定。核心概念讲透，背景概念点到为止。
- **不限制字数**。为深度而长，不为长度而长。

## 每个概念的标准讲法

1. **定义**：一句话说清。术语首次出现用 **英文（中文）** 格式。
2. **机制**：内部怎么运转，讲到具体参数、数据结构、算法步骤。
3. **边界与反例**：什么情况下失效，防止读者把机制神化。
4. **为什么**：它在因果链上的位置，解决了什么、引出了什么。

概念之间用因果链串联：上一个概念解决不了的问题，引出下一个概念。不要并列罗列。

## 文风

- 术语不丢，口语搭桥。口语负责解释"为什么"，不替换术语。
- 承上启下：每个概念的出现都有依据，标题之间有因果衔接。
- 允许下判断，判断要有理由。不编造数据、指标、事故、个人经历。

禁用表达：

- 不用破折号（—— 和 –）
- 不用"逼"这类用力过猛的词，用"引发""导致"
- 不用翻案腔（"不是 A，而是 B"）
- 不用报菜名（"四个词：A、B、C、D"）
- 不用对仗排比
- 不用俗套转折（"你会发现""几乎绕不开"）
- 不用"起点/补救一/钉子一"这类编排腔标题

标题：问题式或机制式；不从正文抽结论句当标题；标题之间读起来能看出论证路径。

## 排版

- Mermaid 图要小：节点 3 到 6 个、标签短，正文讲过的不要重复画。
- 表格用于对比、参数、分档；已全局配置自适应宽度，不在单元格里塞超长文本。
- 提示块（:::）只用于真正值得独立强调的重点或总结。
- 加粗重点词、英文术语首次出现、每节关键结论句。禁止整段加粗、机械加粗段首。
- 分段：一个概念一段或几段，不写一泻千里的大段。

## 代码

- 块前交代场景、输入、目标。
- 块内只在关键分支、状态变化、返回值处写中文注释。
- 块后解释职责、调用顺序、结果、异常和边界。
- 可运行示例必须实际运行验证过；伪代码要明确标注"示意伪代码"。
- Fake Adapter、脚本化模型、内存仓库只能作为测试替身，正文明确它们不能证明真实 API 或供应商行为。

## 事实与安全

- 事实来自官方文档、规范、公开源码、测试，或匿名化的实现证据。
- 私有项目（tp-knowledge、seoOpt 插件、interview-project 等）只能作为只读取证。公开文章不得出现项目名、本机路径、内部接口、表结构、真实数据、提示词、截图或凭证。
- 写作卡、证据台账、自测记录只放 `/tmp`，完成后删除。
- 完稿后从零基础读者和资深工程师两个角度各复读一遍：主题讲清、论证连续、示例可信、语言自然。

## 结构连续性与文章质检

这部分是写作门槛，不是完稿后的装饰性检查。文章即使术语准确、代码可运行，只要论证链断裂，也不能视为完成。

### 一篇文章只能有一条主问题链

- 写作前先写一句“这篇文章要帮读者解决什么问题”，全文围绕这句话推进。
- 核心概念控制在能够讲透的范围内。新概念只有在解决上一个概念留下的问题时才进入正文；与主问题无关的概念拆成其他文章或只留链接。
- 复杂主题可以有多个小节，但不能把定义、架构、工具、测试、生产治理和 FAQ 平铺成知识清单。
- 每篇文章至少保留一个具体任务、故障或数据流作为贯穿案例。案例必须在关键小节中发生状态变化，不能只在开头出现一次。

### 标题树必须读得出论证路径

- 先检查标题树，再检查正文。只读 `#`、`##`、`###` 标题时，读者应能看出“问题是什么、机制如何工作、哪里会坏、下一步为什么出现”。
- 当前标题必须回答上一节留下的问题，或明确缩小到下一个机制。不能从概念突然跳到代码、测试、缓存或生产运维。
- 标题优先使用问题式或机制式表达，例如“它解决了哪一个缺口”“什么情况下会失效”“状态由谁保存”。不要使用只表示分类的标题，例如“相关概念”“其他问题”“一个示例”。
- 一个 `##` 下的 `###` 必须属于同一层论证。若小节可以独立成为另一篇文章，应拆分，而不是在当前文章中顺带介绍。

### 段落之间要有可解释的过渡

- 每个新段落的第一句都要能回答“为什么现在讲这个”。如果只能用“此外”“另外”“同时”连接，通常说明因果关系还没有写清楚。
- 一节结束时不要重复本节结论。用未解决的边界、失败反例或下一个必要动作承接下一节。
- 表格、流程图和代码块不能代替上下文。块前交代它要解决的具体问题，块后解释输出怎样改变后续控制流。
- 文章结尾要回到主问题，并把读者交给下一篇的明确问题。禁止统一套用与主题无关的“机制复核”“迁移复核”“性能与安全权衡”等通用尾段。

### 深度要落到机制，而不是增加名词

- 核心概念至少交代定义、内部机制、一个失败反例和它在主因果链中的作用。
- 代码示例要对应正文刚刚提出的机制，并解释输入、状态变化、输出、异常和边界。不能用一段孤立代码制造“已经深入”的感觉。
- 一篇文章宁可完整推演一次请求、任务、数据结构或故障恢复，也不要用十个小节各介绍一个名词。
- 深度不等于字数。删掉不改变判断、机制或操作的重复解释；新增内容必须带来新证据、新边界或新决策。

### 范文的使用边界

- `docs/ai-agent/llm-workflow-rag-agent.md` 只作为论证深度、叙述节奏、案例推进和排版质量的参照，不是固定目录模板，也不是每篇文章都要复制的开场、表格、提示块和结尾。
- 参照范文时，先提炼它的因果链，再为当前主题重新设计标题树。不能因为范文用了四个概念，就把当前文章也强行扩展成四个概念。
- 不用“统一结构”替代主题判断。文章类型不同，概念文、实现文、诊断文和架构文可以有不同结构，但都必须有清晰的主问题和承接关系。

### 批量重写不能变成模板拼接

- 禁止对多篇文章机械追加相同的总结、质检、迁移或工程建议段落。共享规则应写入本文件，不应复制进每篇正文。
- 批量处理只能用于 frontmatter、链接、格式等确定性修改。正文重写必须逐篇确认主问题、案例、标题树、核心机制和结尾承接。
- 批量完成后不能只检查文件数量、字数、标点和构建。必须抽查标题树、首尾段、案例出现位置、重复段落和跨文章重复结论。
- 发现一篇文章结构失控时，先修写作卡和质检流程，再继续批量写作，不能用更多文章覆盖问题。

### 范文拆解形成的文章论证模型

范文可复用的是论证方法，不是目录模板。重写或新写文章时，至少完成下面的设计：

- **文章契约**：在 `/tmp` 写下主问题、读者读完后的动作、核心案例、核心概念（通常 2 到 4 个）、因果链、失败反例、必须展示的代码或数据结构、明确不讲的内容和下一篇问题。
- **案例状态机**：案例有初始状态，并在关键小节发生状态变化。说明已知事实、刚解决的缺口、仍未解决的边界和下一步由谁决定。不能只在开头举例。
- **标题论证化**：标题树应能读出问题、机制、边界、失败、验证和承接。优先写“解决哪个缺口”“状态由谁维护”“什么情况会失效”，不要把术语排成并列清单。
- **机制六层**：定义、内部运转、输入输出或状态变化、在案例中的作用、失败责任、验证方式。只讲定义和优点不算讲透。
- **横向主线**：按主题选择知识流、控制流、信任流、时间/版本流或请求生命周期。模型、检索、工作流、Agent 类文章优先写清“输入装配 → 调用/执行 → 观察 → 输出解析 → 业务终态”。
- **因果过渡**：每节结尾留下未解决的缺口、失败反例或必要动作，下一节明确接住。若只能用“此外”“另外”“同时”连接，重新检查因果关系。
- **证据块**：图表、代码前说明场景、输入和目标，之后解释职责、控制流、状态变化、异常、边界和测试意义。Fake 模型、内存仓库、适配器只能证明控制逻辑，不能证明真实供应商行为。
- **信任边界**：区分用户输入、外部文档、检索证据、工具回执、模型候选与程序维护的身份、权限、版本、终态。模型建议不等于执行，检索命中不等于有权查看，响应成功不等于事实正确。
- **故障回放**：说明故障属于装配、调用、检索、流程、Agent 循环、输出验证还是业务终态，并记录相关状态、版本、证据和停止原因。不能把所有问题归因于 Prompt。
- **事实分级**：区分实测值、官方约束、经验参数和教学近似。没有证据的阈值、比例、性能数字和行业结论不能写成普遍规律，示例值须标明范围或仅用于演示。
- **结尾闭环**：回答开头主问题，说明解决了什么和仍有什么边界，再提出下一篇要回答的具体问题。禁止批量套用无关的总结或工程建议段落。

## P0 排版与独立第三者门禁

- 普通段落超过约 240 个中文字符、同时承载多个概念或动作、表格单元格塞入长方案、代码/图表前后缺少解释、标题树读不出论证路径时，文章不得交付。
- 桌面正文列保持受控宽度，移动端不得出现页面级横向滚动、文字裁切、目录遮挡或按钮挤压；固定检查 375px、768px、1024px、1440px。
- `docs/ai-agent/llm-workflow-rag-agent.md` 只读，作为质量基线。其余文章必须先完成文章契约、自测，再接受独立第三者检查。
- `yarn article:audit` 检查标题、围栏、段落、重复段落和站内路由；`yarn article:third-party` 从零基础读者、资深工程师、编辑/SEO 三个视角输出 `pass`、`repair_allowed` 或 `rewrite_required`，并提供文件与行号。
- 任一视角为 `rewrite_required` 就推倒重写标题树、案例状态机、因果过渡和结尾；链接、格式、错别字和局部事实标注问题才可局部修复。
- 质检报告、失败草稿、截图和写作卡仅放 `/tmp`，完成后删除。

## 各栏目定位

- **AI 与 Agent**：12 个阶段、98 篇正文的唯一学习主线；AI 实践 6 个阶段、10 篇。`stageKey`、`sequence`、`dependsOn`、`sourceKey`、栏目 Tab、左侧目录、上一篇/下一篇必须来自同一课程模型（`.vitepress/ai-agent-curriculum.ts` 与 `.vitepress/content.ts`）。
- **前端、后端、SEO、AI Infra**：按主题写，重点把概念讲透、让读者能迁移，不追求知识点清单。
- **算法与重学前端**：正文、frontmatter、标题、代码和链接保持不变，只通过站点外部映射接入导航。
- 公开品牌为"小江AI"，不用"课程平台""课程式技术博客"文案。
- 不强制展示统一的"参考资料"章节。

## 流程

1. 写作前确定：文章回答读者的什么问题，核心概念是哪几个，它们的因果链是什么。
2. 复杂主题可在 `/tmp` 建写作卡，完成后删除。
3. 写完自检：概念是否讲到机制层、边界是否交代、段落是否承上启下、代码是否真实可运行。
4. 完稿后用 `article-write` 和 `humanizer-zh` 复核模板腔、过度排比、重复转折、宣传语和过度粗体。
5. 用户只要求质检、审查、对比或讨论原因时，先执行只读审查，不修改正文、标题、导航或样式。报告应先列证据、问题位置、影响范围和根因，再等待用户确认改写范围。
6. 全站审查不能用一篇文章推断全部文章。先按栏目、文章类型和篇幅分组，抽查代表样本，再统计标题树、重复段落、模板尾段、案例贯穿和跨文章重复；未读文章只能标为待审查，不能宣称已完成全面质检。
7. 用户确认改写后，逐篇建立主问题、案例、标题树和因果链，再修改正文。先修一小组样本并复审质量，确认方法有效后再扩展，禁止直接批量覆盖全部文章。

## 站点纪律

- 不执行 `git add`、`git commit`、`git push`。
- 不修改自动部署配置。
- 文章 frontmatter 必须与 `.vitepress/content.ts` 清单一致（title、description、category、part、chapter、tags 等字段），这是站点导航的数据源，不是写作门禁。
