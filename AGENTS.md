# 小江AI 博客写作规范

## 写作前

- 重写或新增长文前必须完整读取 `/Users/mac/.codex/skills/article-write/SKILL.md`，并按文章类型读取其中要求的中文长文参考。
- SEO 文章还必须读取 `/Users/mac/.codex/skills/seo-sem-optimizer/SKILL.md`，并按需核对其 references。
- 复杂主题可在 `/tmp` 建立写作卡，记录读者、核心问题、案例、论点和事实证据。写作卡不进入仓库，完成后删除。
- 私有项目只能作为只读取证。公开文章不得出现项目名、本机路径、内部接口、表结构、真实数据、提示词、截图或凭证。

## 教学质量

- 这是个人学习与实践博客，不是课程平台。文章要让初学者看懂并能迁移到工作。
- 长文围绕一个真实问题、可观察现象或具体实践展开，作者根据主题决定叙述顺序和篇幅。概念解释到什么程度，取决于它在论证中的重要性。
- 机制复杂时可以深入原理，实践性强时可以重点写代码，观点文章可以没有代码。FAQ、故障故事、表格、Mermaid、对比和检查表都是可选表达，不得为了门禁机械添加。
- 不为每个概念重复“定义、输入、输出、验证、边界”等固定结构，也不强制最少章节数、FAQ 数量或表达形式。文章仍应保持论证连续，让读者理解关键结论从何而来。
- AI 与 Agent 文章面向希望达到企业工程标准的开发者。连续开发主线使用同一个匿名只读知识 Agent 和 Python 项目骨架；MCP 专题为了比较跨语言契约，保留 Python 与 Node.js 两种实现。不要在相邻文章中无理由更换场景、对象名或接口。
- 允许作者提出明确判断，但不得编造个人经历、事故、数据、指标或实际效果。
- 重要结论可以用 Markdown 粗体强调；只加粗真正需要记住的短语或短句，禁止整段加粗、机械加粗段首和高密度强调。
- 完稿后使用 `article-write` 和 `humanizer-zh` 复核模板腔、过度排比、重复转折、宣传语和过度粗体。

## 代码与图

- 代码服务于文章论证。作者按理解难度决定篇幅，不按代码数量凑深度；可运行示例必须实际运行。
- AI 与 Agent 的程序和配置代码块前要交代场景、输入和目标，块内在关键分支、状态变化和返回值处使用中文注释，块后解释职责、调用顺序、结果、异常和边界。无需逐行注释，也不能只放一条笼统中文说明。
- Fake Adapter、脚本化模型和内存仓库只能作为测试替身，正文必须明确它们不能证明真实 API、持久化或供应商行为。承诺真实模型调用的文章必须同时给出真实适配器和在线验证条件。
- 配置示例必须说明哪些值需要替换；严格 JSON 不能写注释，带注释的示例使用 `jsonc`。
- Mermaid、表格和图示应帮助理解真实关系，不能只为增加表达形式而出现。

## 事实与审查

- 代码、测试、迁移和运行脚本才能证明“已实现”；设计建议和未实现能力必须明确标记。
- 不编造线上事故、性能数字、用户规模、准确率、上线结果或个人经历。
- 每篇完成后从初学者和资深工程师两个角度复读，检查主题是否讲清、论证是否连续、示例是否可信、语言是否自然。
- 内容锁只保存算法与重学前端文件的完整 SHA-256，不与写作评分或审校状态混用。
- AI 与 Agent 文章不显示顶部“开始前可以了解 / 读完可以带走 / 本文产物”和更新时间，不使用“自测”“迁移练习”“这篇和下一篇怎样衔接”等模板段落。前置关系通过正文中的自然站内链接和接口复用表达。
- AI 与 Agent 使用 12 个阶段、98 篇正文的唯一学习主线；AI 实践使用 6 个阶段、10 篇正文。`stageKey`、`sequence`、`dependsOn`、`sourceKey`、栏目 Tab、左侧目录和上一篇/下一篇必须来自同一课程模型，不维护虚构的产物接力或双轨阅读模式。
- AI 与 Agent 和 AI 实践正文按主题决定结构，但标题树必须能复述问题、机制、实现、验证和边界。每篇至少包含一条由输入、状态变化、调用顺序、输出、失败证据和验证结果组成的完整推演；正文统计排除 Frontmatter、代码、表格、Mermaid、URL 和导航后，至少保留 6000 个中文字符。
- 算法与重学前端正文、frontmatter、标题、代码和链接保持不变，只允许通过站点外部映射接入导航。

## 站点

- 公开品牌为“小江AI”，不得使用“课程平台”“课程式技术博客”等文案。
- 来源只用于写作核实，不在文章中强制展示统一的“参考资料”章节。
- 不执行提交、推送或部署，保持自动部署配置不变。
