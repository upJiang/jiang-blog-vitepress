# 小江AI 博客写作规范

这份规范替代此前所有门禁脚本。它不限制字数、不限制章节数、不限制表达形式，只定义"好文章长什么样"。每篇文章根据主题和内容决定自己的篇幅与结构，避免千篇一律。

## 一、读者与目标

- **每个栏目面向该领域的零基础小白**。写后端文章时，读者是后端小白；写 SEO 文章时，读者是 SEO 小白；写 AI 与 Agent 文章时，读者是想转 AI 应用开发的技术人员。读者有通用编程基础，但不懂该领域的专业概念。
- 不要假设读者是前端、后端或任何特定背景，也不要在一篇后端文章里假设读者懂 LLM、在一篇 AI 文章里假设读者懂数据库。
- 文章要让读者**真正学到东西**：读完之后，他能复述核心机制，能判断边界，能把知识迁移到自己的项目。
- 技术博客，不是课程讲义，也不是聊天记录。专业和通俗要同时在场。
- **写法参照**：`docs/ai-agent/llm-workflow-rag-agent.md` 是写作范本。各栏目写作时参照它的写法：概念纵向挖深的程度、口语与专业的融合方式、承上启下的段落衔接、排版与代码的处理。参照它的写法，而不是复制它的结构。

## 二、深度的定义：纵向挖，不是横向铺

这是全文最重要的一条原则。

- **纵向深度**：把一个概念挖到根。讲它的定义、底层机制、真实参数、边界条件、失败反例。比如讲"切块"，要讲到结构感知切块、表格为什么单独处理、质量门禁有哪些检查项。
- **横向铺开**：为了显得全面，不断引入新概念，每个都只讲两句。这是反面，会让人"看了很多、什么都没懂"。
- 判断标准：**宁可一篇只讲透两个概念，也不要十个概念各一段话。**
- 一个概念要讲到什么程度，由它在文章论证中的重要性决定。核心概念讲透，背景概念点到为止。
- **不限制字数**。内容需要一万字就写一万字，需要两千字就写两千字。为深度而长，不为长度而长。

## 三、每个概念的标准讲法

一个核心概念通常需要这四层，按需要取舍：

1. **定义**：一句话说清它是什么。术语首次出现用 **英文（中文）** 格式，比如 **RAG（Retrieval-Augmented Generation，检索增强生成）**。
2. **机制**：它内部怎么运转。讲到底层，配具体参数、数据结构、算法步骤。这是"深度"的主体。
3. **边界与反例**：它在什么情况下失效。每个重要机制后面都要跟"什么情况下会坏"，防止读者把机制神化。
4. **为什么**：这个概念在整条因果链上的位置，它解决了什么、又引出了什么。

概念之间用**因果链**串联：上一个概念解决不了的问题，引出下一个概念。不要并列罗列。

## 四、文风：口语与专业的融合

- **术语不丢，口语搭桥**。专业术语必须出现、必须被精确解释，口语只负责解释"为什么是这样、这有什么用"，而不是替换术语。
- **承上启下**。作者讲每个概念都要有依据，读者能感受到"为什么现在讲它"。标题之间要有因果衔接，不能突然冒出一个小节。
- **允许下判断**。作者可以有明确立场（"能写死就别上 Agent"），但判断要给出理由。
- **不编造**。不编造数据、指标、事故、个人经历。没有实测的数字就不写数字。

### 禁用表达

- 不用破折号（—— 和 –）。用逗号、句号、冒号。
- 不用"逼"这类用力过猛的词，用"引发""导致"。
- 不用翻案腔："不是 A，而是 B"、"你以为……其实……"。
- 不用报菜名："四个词：A、B、C、D"这种冒号加顿号罗列。
- 不用对仗排比："想清楚了就清楚，想不清楚就纠结"。
- 不用俗套转折："你会发现"、"几乎绕不开"、"值得注意的是"。
- 不用"起点/补救一/钉子一"这类教学编排腔标题。

### 标题

- 用问题式或机制式标题："它解决的是什么"、"什么情况下会坏"。
- 不用从正文里抽结论句当标题。
- 标题之间要有承接关系，读标题树能看出文章的论证路径。

## 五、排版

- **流程图（Mermaid）**：只在真正需要可视化流程时用。图要小：节点少（3 到 6 个为宜）、标签短。正文已经讲清楚的内容，不要再用图重复一遍。
- **表格**：用于对比、参数、分档这类结构化信息。表格样式已全局配置为自适应宽度，不要在一行里塞超长文本。
- **提示块（::: tip / warning / info）**：只用于真正值得独立强调的重点或总结。普通论断融在正文里，不要动不动独立成块。
- **加粗**：加粗重点词、英文术语首次出现、每节的关键结论句。禁止整段加粗、机械加粗段首。
- **分段**：一个概念一段或几段，不要一泻千里的大段落，也不要把完整意思切得太碎。

## 六、代码

- 代码块前交代场景、输入、目标。
- 代码块内只在关键分支、状态变化、返回值处写中文注释，不需要逐行注释。
- 代码块后解释职责、调用顺序、结果、异常和边界。
- 可运行的示例必须实际运行验证过。教学用的伪代码要明确标注"示意伪代码"，不与真代码混同。
- Fake Adapter、脚本化模型、内存仓库只能作为测试替身，正文要明确它们不能证明真实 API 或供应商行为。

## 七、事实与安全

- 事实来自官方文档、规范、公开源码、测试，或匿名化的实现证据。
- 私有项目（tp-knowledge、seoOpt 插件、interview-project 等）只能作为只读取证。公开文章不得出现项目名、本机路径、内部接口、表结构、真实数据、提示词、截图或凭证。
- 写作卡、证据台账、自测记录只放 `/tmp`，完成后删除。
- 完稿后从"零基础读者"和"资深工程师"两个角度各复读一遍：主题是否讲清、论证是否连续、示例是否可信、语言是否自然。

## 八、各栏目定位

- **AI 与 Agent**：12 个阶段、98 篇正文的唯一学习主线，`stageKey`、`sequence`、`dependsOn`、`sourceKey`、栏目 Tab、左侧目录、上一篇/下一篇必须来自同一课程模型（`.vitepress/ai-agent-curriculum.ts` 与 `.vitepress/content.ts`）。文章按主题决定结构，但每篇要把涉及的核心概念讲透，深度参照本规范第二、三条。
- **AI 实践**：6 个阶段、10 篇正文，同上约束。
- **前端、后端、SEO、AI Infra**：按各自栏目的主题写，重点是把每个概念讲透、让读者能迁移到工作，而不是覆盖知识点清单。
- **算法与重学前端**：正文、frontmatter、标题、代码和链接保持不变，只允许通过站点外部映射接入导航。
- 公开品牌为"小江AI"，不使用"课程平台""课程式技术博客"等文案。
- 来源只用于写作核实，不在文章中强制展示统一的"参考资料"章节。

## 九、流程

1. 写作前确定：这篇文章回答读者的什么问题，核心概念是哪几个，它们之间的因果链是什么。
2. 复杂主题可在 `/tmp` 建写作卡，记录读者、核心问题、案例、论点和事实证据。完成后删除。
3. 写完自检：概念是否讲到了机制层、边界是否交代、段落是否承上启下、代码是否真实可运行。
4. 完稿后用 `article-write` 和 `humanizer-zh` 复核模板腔、过度排比、重复转折、宣传语和过度粗体。

## 十、站点纪律

- 不执行 `git add`、`git commit`、`git push`。
- 不修改自动部署配置。
- 文章的 frontmatter 必须与 `.vitepress/content.ts` 里的清单一致（title、description、category、part、chapter、tags 等字段），这是站点导航的数据源，不是写作门禁。
