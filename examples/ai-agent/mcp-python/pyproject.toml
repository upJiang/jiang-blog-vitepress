[project]
name = "blog-mcp-python-example"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "mcp[cli]>=2,<3",
]

[dependency-groups]
dev = [
  "pytest>=8,<9",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

