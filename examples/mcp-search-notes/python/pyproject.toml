[project]
name = "mcp-search-notes-python"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
  "jsonschema==4.26.0",
  "mcp==2.0.0",
]

[dependency-groups]
dev = [
  "pytest>=8.4,<9",
  "pytest-asyncio>=1.2,<2",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
