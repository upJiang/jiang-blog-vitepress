meta {
  name: live
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/health/live
  body: none
  auth: none
}

tests {
  test("returns health", function() {
    expect(res.status).to.equal(200)
    expect(res.body.status).to.equal("ok")
  })
}
